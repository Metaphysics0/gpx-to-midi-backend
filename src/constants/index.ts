export const SUPPORTED_FILE_TYPES = ['gp', 'gp4', 'gp5', 'gp6', 'gp7'];

export enum ConvertOptionsType {
  GUITAR_PRO = 'gp',
  MIDI = 'midi',
}
export const CONVERT_OPTIONS = Object.values(ConvertOptionsType);
